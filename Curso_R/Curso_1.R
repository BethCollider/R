#mydata <- read.csv("/Users/werito/Desktop/DAP_ALT_VOL.csv", header=TRUE)
#mydata <- na.omit(mydata)
#mydata


numero <- 1.25 # Vector : archivo que se almacena en la memoria de la CP 
class(numero)

x <- 2L # es un numero Entero ya que no tiene decimales
class(x)

numeros <- c (1.25,3.5,2.3,9.7, 5L)
class(numeros)

caracter <- "jaime"
nombres <- c ("pedro", "veronica", "Juan", "Pedro", "Jaime")

salud <- c("Mala", "Buena", "Muy Mala", "Regular", "Muy Buena")

V1 <- c(1,2,3,4,"A")

V2 <- c(1,2,3,4,F)